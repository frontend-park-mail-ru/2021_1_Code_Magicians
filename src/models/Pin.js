import {Model} from './Model.js';

/**
 * Pin model
 */
export class Pin extends Model {
  /**
   * Constructor
   * @param {Object} props : {
   *   ID,
   *   boardID,
   *   title,
   *   description,
   *   tags,
   * }
   */
  constructor(props) {
    super(props);
  }
}
