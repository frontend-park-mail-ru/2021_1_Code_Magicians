import {Model} from '../Model';

/**
 * Tag model
 */
export class Tag extends Model {
  /**
   * Constructor
   * @param {Object} props : {
   *   ID,
   *   name,
   * }
   */
  constructor(props) {
    super(props);
  }
}
